# dsh-image-vision-bridge 的 bundle 补丁层:安装本包时自动插入插件行。
# 用户可以在自己的 profile cordis.patch.yml 里按 id 覆盖这些配置
# (例如改 provider/model,或把 enabled 设为 false)。
- insert:
    - id: image-vision-bridge
      name: 'dsh-image-vision-bridge'
      config:
        provider: opencode-go
        model: mimo-v2.5
